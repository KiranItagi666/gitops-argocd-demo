apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-argocd-demo-chart.fullname" . }}
  labels:
    {{- include "gitops-argocd-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-argocd-demo-chart.selectorLabels" . | nindent 4 }}
